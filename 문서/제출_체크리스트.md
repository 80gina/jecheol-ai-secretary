# 제출 체크리스트 — 1차 미션

## AI Agent 개발 : 나만의 AI 비서 구축

> 미션 명세(1차미션.pdf)의 항목을 그대로 옮기고, 각 항목이 **어디서 확인되는지**를 적었습니다.
> 배포 관련 항목은 `문서/실행검증.md` 가 실제 HTTP 응답으로 뒷받침합니다.

| | |
|---|---|
| 최종 확인 | 2026-09-19 |
| 저장소 | https://github.com/80gina/jecheol-ai-secretary |
| 프론트 | https://jecheol-ai-secretary.vercel.app |
| 백엔드 | https://seasonal-ai-backend.onrender.com |
| Swagger | https://seasonal-ai-backend.onrender.com/docs |

---

## A. 최종 결과물 4가지 (명세 2장)

| # | 요구 | 상태 | 확인 위치 |
|:-:|---|:--:|---|
| 1 | **데이터 기반 AI 채팅** — 자연어 질문 → 저장 데이터 요약 반영 답변 + 로딩 표시 | ✅ | `POST /api/chat` · 응답의 `summary_used` 가 답변 근거. 로딩 표시는 `frontend/app.js` |
| 2 | **데이터 관리(CRUD)** — (date, value, memo) 추가/수정/삭제 + 목록 갱신 | ✅ | `/api/data` 4종 · 화면 「데이터 관리」 |
| 3 | **대화 기록 저장 및 불러오기** — 저장·목록·특정 대화 재표시 | ✅ | `/api/conversations` 3종 + `GET /api/conversations/{id}` (A안) |
| 4 | **배포 및 문서화** — 배포 URL 접속 + Swagger + README | ✅ | 위 URL 3종 · `README.md` |

---

## B. 기능 요구 사항 10항목 (명세 4장)

### 1. 개발 환경 준비

| 요구 | 상태 | 확인 위치 |
|---|:--:|---|
| Python 3.10 이상 + venv | ✅ | `PYTHON_VERSION=3.11.9` (render.yaml) |
| fastapi, uvicorn, firebase-admin, openai, python-dotenv | ✅ | `backend/requirements.txt` |
| Firebase 프로젝트 + 서비스 계정 키 | ✅ | `FIREBASE_SERVICE_ACCOUNT_B64` (base64, 저장소에 없음) |
| LLM API 키 | ✅ | `OPENAI_API_KEY` — 코디세이 게이트웨이 OpenAI 호환 키 |
| Render / Vercel 계정 | ✅ | 양쪽 배포 완료 |

### 2. 데이터 선정 및 분석

| 요구 | 기준 | 실제 | 확인 위치 |
|---|---|---|---|
| 시계열 데이터 선정 | — | 제철 식재료 12품목 일별 소매가 | `backend/data/seed_data.json` |
| 데이터 포인트 | 100개 이상 | **8,760건** (12품목 × 730일) | `GET /api/data/summary` 의 `count` |
| 요약 정보 산출 | 기간·개수·통계·추세 | `period` / `count` / `metrics` / `trend` 전부 포함 | `GET /api/data/summary` |

### 3. FastAPI 프로젝트 구성

| 요구 | 상태 | 확인 위치 |
|---|:--:|---|
| 앱 초기화 + CORS 설정 | ✅ | `backend/app/main.py` |
| 로컬 실행 확인 | ✅ | `uvicorn app.main:app --reload` |
| Swagger UI(`/docs`) | ✅ | 배포 URL + `/docs` · `문서/실행검증.md` 3절 |

### 4. Firebase(Firestore) 연동

| 요구 | 상태 | 확인 위치 |
|---|:--:|---|
| Firestore Database 사용 | ✅ | `backend/app/db.py` · `/health` 의 `db_backend` |
| 서비스 계정 키를 환경 변수로 | ✅ | `app/config.py` — 코드 내 하드코딩 0건 |
| 컬렉션 설계 (`data`, `conversations`) | ✅ | `db.py` 의 상수 2개 |

### 5. 데이터 API (5개)

| # | 엔드포인트 | 상태 |
|:-:|---|:--:|
| 1 | `POST /api/data` | ✅ |
| 2 | `GET /api/data` | ✅ |
| 3 | `PUT /api/data/{id}` | ✅ |
| 4 | `DELETE /api/data/{id}` | ✅ |
| 5 | `GET /api/data/summary` | ✅ |

### 6. 대화 기록 API

| # | 엔드포인트 | 상태 |
|:-:|---|:--:|
| 1 | `POST /api/conversations` | ✅ |
| 2 | `GET /api/conversations` | ✅ (messages 미포함으로 명시) |
| 3 | `DELETE /api/conversations/{id}` | ✅ |
| — | 불러오기 **(A)** `GET /api/conversations/{id}` — 전체 messages 조회 | ✅ |

### 7. AI 챗봇 API (컨텍스트 주입)

| 동작 흐름 | 상태 | 확인 위치 |
|---|:--:|---|
| ① 데이터 요약 조회 | ✅ | `chat_service.chat()` 1단계 |
| ② 요약을 시스템 프롬프트에 삽입 | ✅ | 2단계 · `build_system_prompt()` |
| ③ LLM 호출 | ✅ | 3단계 |
| ④ 대화 자동 저장 | ✅ | 4단계 — 응답의 `conversation_id` |

### 8. 백엔드 배포 (Render)

| 요구 | 상태 | 확인 위치 |
|---|:--:|---|
| GitHub 푸시 | ✅ | 저장소 |
| Render Web Service 배포 | ✅ | `Deploy succeeded / Live` |
| 배포 URL에서 Swagger 확인 | ✅ | `문서/실행검증.md` — HTTP 200 기록 |
| 콜드스타트 대응 | ✅ | `/health` 선호출 + 안내 배너. 실측 32.5초 / 36.7초 기록 |

### 9. 웹 프론트엔드 (바닐라)

| 요구 | 상태 | 확인 위치 |
|---|:--:|---|
| HTML/CSS/JS만 사용 (프레임워크 금지) | ✅ | 프레임워크·차트 라이브러리 **0개** |
| 채팅 인터페이스 (입력·표시·로딩) | ✅ | `frontend/index.html` |
| 데이터 관리 (추가·목록·수정/삭제 중 1개 이상) | ✅ | 화면에서 동작 확인 |
| 대화 기록 (목록·불러오기) | ✅ | 화면에서 동작 확인 |
| 데이터 요약 표시 (기간·개수·트렌드) | ✅ | 상단 요약 영역 |

### 10. 프론트엔드 배포(Vercel) 및 문서화

| 요구 | 상태 | 확인 위치 |
|---|:--:|---|
| Vercel 배포 + 환경 변수로 API 주소 설정 | ✅ | `frontend/build.js` 가 `API_BASE_URL` 주입 |
| README — 서비스 소개 | ✅ | `README.md` 1절 |
| README — 기술 스택 | ✅ | 2절 |
| README — 배포 URL 3종 | ✅ | 3절 |
| README — 로컬 실행 방법 | ✅ | 4절 |
| README — 환경 변수 목록 | ✅ | 5절 |
| 제출 스크린샷 — 채팅(질문+답변) | ⬜ **직접** | 배포 화면에서 촬영 |
| 제출 스크린샷 — 데이터 관리(CRUD 1개) | ⬜ **직접** | |
| 제출 스크린샷 — 대화 기록(불러오기) | ⬜ **직접** | |

---

## C. 보너스 과제 (명세 5장)

### 보너스 ① Function Calling + 멀티채널

| 요구 | 상태 | 확인 위치 |
|---|:--:|---|
| 내부 기능을 도구로 정의·연결 | ✅ | 도구 **11종** · `GET /api/tools` |
| MCP Server 또는 GPT Actions 중 1개로 연동 검증 | ✅ | `backend/mcp_server/server.py` |
| README에 호출 근거와 흐름 정리 | ✅ | README 보너스 절 |

### 보너스 ② 인사이트·UX 고도화

| 요구 | 상태 | 확인 위치 |
|---|:--:|---|
| 추가 지표 1개 이상 | ✅ | `/api/data/statistics`, `/scores`, `/swot`, `/forecast` |
| 프론트 시각화 1개 이상 | ✅ | SVG 직접 구현 (라이브러리 없음) |
| 데이터 내보내기 (CSV/JSON) | ✅ | `GET /api/data/export?format=` |
| 다크 모드 토글 | ✅ | 3단 색 정의 — `문서/반응형-검증.md` |

---

## D. 제약 사항 (명세 7장)

| 요구 | 상태 | 확인 위치 |
|---|:--:|---|
| 백엔드는 Render | ✅ | |
| 프론트엔드는 Vercel | ✅ | |
| 키를 환경 변수로, 코드에 노출 금지 | ✅ | `.gitignore` 에 `.env`·서비스 계정 키. `.env.example` 은 이름만 공개 |
| 입력 값 검증 + 예외 처리 | ✅ | Pydantic 스키마 + 라우터 예외 처리 |
| `OPENAI_API_KEY` | ✅ | Render 환경 변수 |
| `FIREBASE_SERVICE_ACCOUNT_JSON`(또는 경로) | ✅ | `FIREBASE_SERVICE_ACCOUNT_B64` 로 구현 |
| `API_BASE_URL` | ✅ | Vercel 빌드 시 주입 |
| `ALLOWED_ORIGINS` | ✅ | `/health` 응답에 표시 |
| 비용 주의 — 토큰 제한 | ✅ | `OPENAI_MAX_TOKENS=1500` |
| 비용 주의 — 작은 데이터로 먼저 검증 | ✅ | `smoke_test.py` (LLM 호출 없이 동작) |

---

## E. 제출 전 남은 일

| # | 할 일 | 비고 |
|:-:|---|---|
| 1 | 스크린샷 3종 촬영 | 채팅 / 데이터 관리 / 대화 기록 |
| 2 | `문서/반응형-검증.md` 측정표 채우기 | 4개 해상도 |
| 3 | 사용 안 하는 키 폐기 | 이전에 발급한 키 정리 |

---

## F. 근거 문서

| 문서 | 내용 |
|---|---|
| `문서/결과보고서.md` | 본 보고서 — 평가 목표 6개에 대한 설명 |
| `문서/실행검증.md` | 배포 상태를 실제 HTTP 응답으로 기록 (자동 생성) |
| `문서/반응형-검증.md` | 브레이크포인트 설계와 측정 |
| `문서/시연-시나리오.md` | 시연 순서 |
| `docs/openapi.json` | 엔드포인트 명세 (자동 생성) |
| `README.md` | 서비스 소개·기술 스택·실행 방법·환경 변수 |

> 평가는 **코드와 텍스트만** 읽습니다. 이미지·PDF는 대상이 아니므로,
> 증빙은 모두 `.md` 와 `.json` 으로 남겼습니다.
